# Filesystem MCP Server Environment Variables
# No environment variables required for basic usage

# Optional: Set allowed directories via environment (if supported by wrapper)
# FILESYSTEM_ALLOWED_DIRS=/path/to/dir1:/path/to/dir2
