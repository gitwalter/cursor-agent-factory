/**
 * ${SCHEMA_NAME} Schema - Main CDS Schema Definition
 * 
 * Purpose: Define namespace and import entities for ${DOMAIN_NAME}
 * Author: ${AUTHOR}
 * Created: ${DATE}
 * 
 * This is the main schema file that defines the namespace and imports
 * all entity definitions. It serves as the entry point for the data model.
 * 
 * @see https://cap.cloud.sap/docs/guides/domain-modeling#namespaces
 */

/**
 * Define the namespace for this domain.
 * 
 * Namespaces help organize entities and prevent naming conflicts.
 * Use reverse domain notation: com.company.domain
 */
namespace ${NAMESPACE};

/**
 * Import common aspects and types from CAP standard library
 */
using {
    cuid,
    managed,
    temporal,
    Country,
    Currency,
    Language,
    localized
} from '@sap/cds/common';

/**
 * Import entity definitions from other files
 * 
 * Organize entities into logical files and import them here.
 * This keeps the schema maintainable as it grows.
 */
// using { ${ENTITY1_NAME} } from './entities/${ENTITY1_FILE}';
// using { ${ENTITY2_NAME} } from './entities/${ENTITY2_FILE}';
// using { ${ASPECT_NAME} } from './aspects/${ASPECT_FILE}';

/**
 * Define common types and enums for reuse across entities
 */
type ${ENUM_TYPE_NAME} : String(50) enum {
    ${ENUM_VALUE1};
    ${ENUM_VALUE2};
    ${ENUM_VALUE3};
};

/**
 * Define custom types for common patterns
 */
type ${CUSTOM_TYPE_NAME} : String(${TYPE_LENGTH:255});

/**
 * Example entity definition (move to separate file for larger schemas)
 * 
 * This is a placeholder - replace with your actual entity definitions
 * or import them from separate files.
 */
entity ${EXAMPLE_ENTITY_NAME} : cuid, managed {
    key ID : UUID;
    name : String(255);
    description : String(1000);
}
