/**
 * ${ASPECT_NAME} Aspect - Reusable CDS Aspect Definition
 * 
 * Purpose: Define reusable aspect for common fields across entities
 * Author: ${AUTHOR}
 * Created: ${DATE}
 * 
 * Aspects allow you to define common field structures that can be
 * reused across multiple entities, reducing duplication.
 * 
 * @see https://cap.cloud.sap/docs/guides/domain-modeling#aspects
 */

namespace ${NAMESPACE};

/**
 * ${ASPECT_NAME} aspect - reusable field structure.
 * 
 * Aspects define a set of fields that can be included in entities
 * using the 'aspects' keyword. This promotes DRY (Don't Repeat Yourself)
 * principles and ensures consistency across entities.
 * 
 * Usage:
 * ```cds
 * entity MyEntity : ${ASPECT_NAME} {
 *     // Additional fields specific to MyEntity
 * }
 * ```
 */
aspect ${ASPECT_NAME} {
    /**
     * Common field 1 - replace with your actual common fields
     */
    ${FIELD1_NAME} : String(${FIELD1_LENGTH:255});

    /**
     * Common field 2 - replace with your actual common fields
     */
    ${FIELD2_NAME} : String(${FIELD2_LENGTH:1000});

    /**
     * Common enum field example
     */
    // ${ENUM_FIELD_NAME} : String(50) enum {
    //     VALUE1;
    //     VALUE2;
    //     VALUE3;
    // };

    /**
     * Common calculated field example
     */
    // ${CALCULATED_FIELD_NAME} : String(255) default '${DEFAULT_VALUE}';
}

/**
 * Example: Using the aspect in an entity
 * 
 * entity ExampleEntity : ${ASPECT_NAME} {
 *     key ID : UUID;
 *     // Automatically includes all fields from ${ASPECT_NAME}
 *     // Add entity-specific fields here
 * }
 */
