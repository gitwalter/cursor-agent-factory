/**
 * Loading Component - Next.js Loading UI
 * 
 * Purpose: Display loading state while page data is being fetched
 * Author: {{AUTHOR}}
 * Created: {{DATE}}
 * 
 * Axiom Alignment:
 * - A1 (Verifiability): Loading states are clearly indicated
 * - A3 (Transparency): Users understand what is happening
 * - A4 (Non-Harm): Prevents layout shift and provides feedback
 */

import { {{SKELETON_COMPONENT}} } from '@/components/ui/{{SKELETON_NAME}}'

/**
 * Loading component displayed during page data fetching.
 * 
 * This component is automatically shown by Next.js while
 * the page component is loading. It helps prevent layout
 * shift and provides user feedback.
 * 
 * @returns Loading UI element
 */
export default function Loading() {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center p-24">
      <div className="z-10 max-w-5xl w-full">
        {/* Skeleton loader */}
        <{{SKELETON_COMPONENT}} />
        
        {/* Optional: Add loading spinner */}
        <div className="flex justify-center items-center mt-8">
          <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-gray-900"></div>
        </div>
        
        {/* Loading message */}
        <p className="text-center mt-4 text-gray-600">
          {{LOADING_MESSAGE}}
        </p>
      </div>
    </div>
  )
}