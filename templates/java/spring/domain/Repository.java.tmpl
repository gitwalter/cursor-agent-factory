package {{PACKAGE_NAME}}.domain;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;
import org.springframework.stereotype.Repository;

import java.util.List;
import java.util.Optional;

/**
 * {{CLASS_NAME}} - Repository Interface
 * 
 * Purpose: Data access layer for {{DOMAIN_NAME}} entities
 * Author: {{AUTHOR}}
 * Created: {{DATE}}
 * 
 * Extends Spring Data JPA repository for CRUD operations.
 * Add custom query methods as needed.
 */
@Repository
public interface {{CLASS_NAME}}Repository extends JpaRepository<{{CLASS_NAME}}, Long> {

    /**
     * Find {{DOMAIN_NAME}} by {{FIELD_NAME}}.
     * 
     * @param {{FIELD_VAR_NAME}} Field value to search for
     * @return Optional containing entity if found
     */
    Optional<{{CLASS_NAME}}> findBy{{FIELD_NAME_CAPITALIZED}}(String {{FIELD_VAR_NAME}});

    /**
     * Check if {{DOMAIN_NAME}} exists by {{FIELD_NAME}}.
     * 
     * @param {{FIELD_VAR_NAME}} Field value to check
     * @return true if exists, false otherwise
     */
    boolean existsBy{{FIELD_NAME_CAPITALIZED}}(String {{FIELD_VAR_NAME}});

    /**
     * Find all {{DOMAIN_NAME}} entities by {{FIELD_NAME}}.
     * 
     * @param {{FIELD_VAR_NAME}} Field value to search for
     * @return List of matching entities
     */
    List<{{CLASS_NAME}}> findAllBy{{FIELD_NAME_CAPITALIZED}}(String {{FIELD_VAR_NAME}});

    /**
     * Custom query example: Find {{DOMAIN_NAME}} by custom criteria.
     * 
     * @param param1 First parameter
     * @param param2 Second parameter
     * @return List of matching entities
     */
    @Query("SELECT e FROM {{CLASS_NAME}} e WHERE e.field1 = :param1 AND e.field2 = :param2")
    List<{{CLASS_NAME}}> findCustomQuery(
            @Param("param1") String param1,
            @Param("param2") String param2);

    /**
     * Native query example: Complex database-specific query.
     * 
     * @param value Parameter value
     * @return List of matching entities
     */
    @Query(value = "SELECT * FROM {{TABLE_NAME}} WHERE column = :value", nativeQuery = true)
    List<{{CLASS_NAME}}> findNativeQuery(@Param("value") String value);
}
