package {{PACKAGE_NAME}}.domain;

import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.NotNull;
import jakarta.validation.constraints.Size;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;

import java.time.LocalDateTime;

/**
 * {{CLASS_NAME}} - Data Transfer Objects (DTOs)
 * 
 * Purpose: Request/Response DTOs for {{DOMAIN_NAME}} operations
 * Author: {{AUTHOR}}
 * Created: {{DATE}}
 * 
 * Separates API contracts from domain entities.
 * Includes validation annotations for input validation.
 */

/**
 * Request DTO for creating a new {{DOMAIN_NAME}}.
 */
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
class {{CLASS_NAME}}CreateRequestDto {

    @NotBlank(message = "Name is required")
    @Size(max = 255, message = "Name must not exceed 255 characters")
    private String name;

    @Size(max = 1000, message = "Description must not exceed 1000 characters")
    private String description;

    // TODO: Add other required fields with validation
}

/**
 * Request DTO for updating an existing {{DOMAIN_NAME}}.
 */
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
class {{CLASS_NAME}}UpdateRequestDto {

    @NotBlank(message = "Name is required")
    @Size(max = 255, message = "Name must not exceed 255 characters")
    private String name;

    @Size(max = 1000, message = "Description must not exceed 1000 characters")
    private String description;

    // TODO: Add other updatable fields with validation
}

/**
 * Response DTO for {{DOMAIN_NAME}} operations.
 * Contains read-only data returned to clients.
 */
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
class {{CLASS_NAME}}ResponseDto {

    @NotNull
    private Long id;

    @NotNull
    private String name;

    private String description;

    @NotNull
    private {{CLASS_NAME}}.{{CLASS_NAME}}Status status;

    @NotNull
    private LocalDateTime createdAt;

    @NotNull
    private LocalDateTime updatedAt;

    // TODO: Add other response fields
}

/**
 * Summary DTO for list views (lightweight version).
 * Use when full entity details are not needed.
 */
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
class {{CLASS_NAME}}SummaryDto {

    @NotNull
    private Long id;

    @NotNull
    private String name;

    @NotNull
    private {{CLASS_NAME}}.{{CLASS_NAME}}Status status;

    // TODO: Add minimal fields needed for list views
}
