# Sample YAML configuration for testing
# This file is used by integration tests

project_name: test-yaml-project
project_description: A test project created from YAML configuration
domain: testing
primary_language: python
frameworks:
  - pytest
  - pyyaml
triggers:
  - manual
agents:
  - code-reviewer
skills:
  - bugfix-workflow
  - tdd
mcp_servers:
  - name: test-server
    url: https://test.example.com
    purpose: Testing purposes
style_guide: pep8
